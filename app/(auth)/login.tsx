/**
 * ============================================
 * v0.1 리팩터: 로그인 화면 중복 제거
 * ============================================
 * - Guardian의 로그인 UX는 온보딩 3페이지(step3)와 동일하다.
 * - 기존 경로 `/(auth)/login`은 하위 호환을 위해 alias로 유지한다.
 */
export { default } from "../onboarding/step3";
